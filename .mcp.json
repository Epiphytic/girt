{
  "mcpServers": {
    "girt": {
      "command": "cargo",
      "args": ["run", "--manifest-path", "${CLAUDE_PLUGIN_ROOT}/Cargo.toml", "--bin", "girt", "--", "--wassette-bin", "wassette"],
      "env": {
        "GIRT_LOG": "info"
      }
    }
  }
}
